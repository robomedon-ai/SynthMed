# --- Markdown (keep only README.md) ---
*.md
!README.md

# --- PDFs ---
*.pdf

# --- Datasets / raw & preprocessed data ---
Placenta accreta spectrum disorders(PASDs)/
Placenta accreta spectrum disorders(PASDs).zip
prostate-cancer-grade-assessment/
prostate158_preprocessed/
topbrain_preprocessed/
tstr_synth_data/
pasd_synth/
annotations/

# Heavy data file types (datasets / generated images)
*.tiff
*.tif
*.gz
*.rar
*.zip

# --- Trained models / checkpoints ---
cv_models/
pasd_models/
prostate_models/
tstr_synth_models/
*.pt
*.pth
*.ckpt
*.safetensors

# --- Python caches / logs ---
__pycache__/
*.pyc
*.log

# --- LaTeX paper folder ---
paper/

# --- Paper sources & analysis artifacts (not part of the app) ---
paper_final2/
preval_reference/
rerun_logs/
TRANSFER_STATUS.txt

# --- Local editing backups ---
*.bak*
*.PRE*

# --- Exception: the paper's supplementary material is published with the code,
# because the camera-ready footnote points readers to this repository for it.
!supplementary.pdf
